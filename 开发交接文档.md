# PikPak Desktop 开发目标与 Agent 交接清单

> 最后更新：2026-09-09  
> 当前源码版本：`0.55.18`
> 工作目录：`D:\dev\pikpak`
> GitHub：`yunjohn/pikpak`  
> 本文档随源码维护并提交，用于后续开发交接；不得写入账号、令牌、Cookie、真实接口响应等敏感信息。

## 1. 产品目标与完成标准

目标是做一个可长期日常使用的 Windows/macOS PikPak 桌面客户端，而不是只完成界面演示。当前开发与实机打包重点为 Apple Silicon macOS。

完成标准：

- 用户可通过官方扫码登录，登录成功后主窗口自动进入文件页。
- 退出程序并重新打开后仍保持登录；访问令牌过期时能自动续期，不要求频繁重新扫码。
- 文件浏览、搜索、上传、下载、离线下载、分享、图片预览、音视频播放均通过真实账号验证。
- 网络失败、权限失败、令牌失效和接口变化都有可理解的提示及恢复路径。
- 安装、升级、日志、崩溃诊断和版本识别达到可交付水平。
- 自动化测试、构建检查与真实账号验收同时通过，不能只以测试桩通过作为完成依据。

## 2. 不可回归的产品约束

后续 Agent 修改前必须保留这些行为：

1. 文件浏览不增加左侧目录树。
2. 双击文件用于打开或预览，不能直接触发下载。
3. 不恢复旧版右侧预览栏。
4. 文件操作尽量在当前列表上下文完成。
5. 页面切换继续使用 Vue 状态导航，避免整页刷新。
6. 保留面包屑导航与当前目录语义。
7. 不把 token、cookie、账号信息或真实接口响应提交到 Git。
8. 不绕过官方登录页，不在应用中收集用户密码。
9. 不以猜测的接口字段替代真实账号验证；不确定的兼容字段需集中封装。
10. 不覆盖或清理用户已有的未提交改动。

## 3. 当前工作区与发布状态

### Git 状态

- 当前基线提交：`2be42c6 Add lazy video playlist and complete web login`
- `origin/main` 与该提交一致。
- 工作树是脏的，包含从 `0.53.7` 到 `0.55.14` 的认证、传输、查看器、macOS 手势、安全、交互与工程化改动，不得直接丢弃或用 `git reset --hard` 覆盖。
- 已修改文件主要包括：
  - `electron/core.cjs`
  - `electron/main.cjs`
  - `electron/preload.cjs`
  - `electron/viewer.js`
  - `electron/viewer.css`
  - `electron/logger.cjs`
  - `src/App.vue`
  - `src/style.css`
  - `index.html`
  - `test/*.test.js`
  - `.github/workflows/ci.yml`
  - `package.json`
  - `package-lock.json`
- `release/` 中包含多个本地候选版本。不要提交，也不要在未确认精确路径时批量删除。

### 最新 macOS 安装包

> 说明：源码版本与最近一次已打包产物均为 `0.55.14`，包含播放恢复、剪贴板磁力链自动下载和原生外部搜索等改动。

`package:mac` 已固定使用 `node_modules/electron/dist` 的本地 Electron 运行时，避免 electron-builder 在网络不稳定时重新下载 Electron 导致 TLS 连接中断。执行 `npm install` 后可直接运行 `npm run package:mac`。

- 版本：`0.55.14`，架构：Apple Silicon (`arm64`)，未签名、未公证。
- DMG：`/Users/xu/Documents/pikpak/release/PikPak-Desktop-0.55.14-mac-arm64.dmg`
  - 大小：`146197403` 字节
  - SHA-256：`fc2d497c51fbdb5c5f8dcb18ca488366feeb11c458227e9bc1761515105e9db4`
- ZIP：`/Users/xu/Documents/pikpak/release/PikPak-Desktop-0.55.14-mac-arm64.zip`
  - 大小：`143373064` 字节
  - SHA-256：`6cdadfd8e7122609fee37de09020ed2974c4b7a28202fb052dd412e22ed5faa2`

### 已有验证证据

- Vitest：11 个测试文件，147 项测试通过（`fixtures`/`token-expiry`/`logger`/`offline`/`scale`/`version`/`core`/`navigation`/`transfer`/`ui-structure`/`viewer`）。
- 构建：`npm run build` 通过（vite 构建、HLS 打包、相对路径核对）。
- IPC 构建检查：preload 与 main 共核对 67 个通道，通过（含 `offline:clipboard` 和外部搜索相关通道）。
- `0.55.13` arm64 应用冷启动烟雾测试通过，主窗口、导航和文件区域可正常出现；截图时文件列表仍在加载，不能据此视为真实账号 API 验收。
- `0.55.14` 的 `hdiutil verify` 与 `unzip -tq` 均通过。
- `0.55.15` Windows x64 便携版已在 Windows 主机完成生产构建与隔离资料目录的冷启动烟雾测试；产物未签名，真实账号验收仍待用户执行。
- 用户已确认 `0.50` 的首次扫码登录可以成功，但登录状态会在数小时后失效；用户已确认离线下载的“添加、删除”在真实环境下可用。
- `0.53.7` 的认证请求拦截器回归已修复；当前登录状态机、令牌提前刷新、隐藏官方会话兜底均已实现并有单元/契约测试。
- 数小时后的自动续期尚未完成真实时间跨度验收。

## 4. 功能状态总览

| 模块 | 当前状态 | 下一步验收或完善 |
|---|---|---|
| 官方扫码登录 | 已实现状态机与关键回归修复 | 真实账号重新扫码，确认自动跳转 |
| 会话持久化 | 已保存授权头、刷新令牌和过期时间 | 重启、跨数小时、刷新失败回退验收 |
| 文件列表与面包屑 | 已实现分页、状态恢复和 macOS 手势后退保护 | 真实账号大目录、异常与权限场景 |
| 搜索 | 已实现全盘遍历、取消旧请求、2 分钟缓存、变更失效和安全高亮 | 真实账号大目录性能 |
| 文件操作 | 已实现主要入口 | 真实账号逐项验证移动、复制、重命名、删除、恢复 |
| 上传 | 已实现进度、取消、重试、并发、分片续传和目录上传 | 真实账号大文件与重名验收 |
| 下载 | 已实现进度、取消、重试、断点续传、链接刷新和落盘大小校验 | 真实账号大文件与重定向验收 |
| 图片预览 | 已支持最多 5000 项、邻近预加载、缓存淘汰、拖动和捏合缩放 | 真实大目录与损坏图片验收 |
| 视频/音频播放 | 已实现懒加载播放列表、URL 刷新、字幕样式/延迟、手势、独立控制条和编码错误提示 | 真实编码、长视频与字幕验收 |
| 外部播放器 | 已支持 macOS `.app`、POSIX 可执行文件和含空格路径 | VLC、IINA、mpv 真实播放验收 |
| 分享 | 有入口和基础能力 | 创建、取消、访问、过期、权限真实验收 |
| 离线下载 | 已支持创建、分页查询、10 秒状态同步和删除，并纳入统一传输中心 | 暂停、继续、重试及真实账号验收 |
| 设置与日志 | 已有设置、版本/构建信息、统一脱敏和诊断 JSON 导出 | 设置迁移与崩溃文件收集 |
| 安装与升级 | 已生成并验证 macOS arm64 DMG/ZIP，CI 覆盖 macOS/Windows | Apple/Windows 签名、自动更新、回滚 |

## 5. 当前最高优先级：登录回归与长期有效性

### 已知事实

1. 用户确认 `0.50` 可以完成首次登录，但数小时后失效。
2. `0.51+` 的一个关键回归是 `session.webRequest.onBeforeRequest` 监听 `/v1/auth/token` 后没有调用 Electron 回调，官方 token 请求会一直挂起。
3. `0.53.7` 已在所有分支通过 `finally { callback({}) }` 放行该请求。
4. CDP 登录响应拦截已经移除；首次登录恢复为接近 `0.50` 的路径，主要依赖官方页面产生的 `Authorization` 请求头。
5. 只有捕获到 access token 后才延迟读取页面存储，避免登录过程中主动脚本干扰官方页面。
6. 已加入 JWT 过期时间计算、提前 5 分钟刷新、刷新令牌失败后使用隐藏官方会话兜底。

### 下一位 Agent 的执行顺序

1. 先确认用户实际运行的是最新 `0.55.14` 安装版的完整路径，避免旧进程或旧安装版混淆。
2. 请用户彻底退出所有旧版本进程，再启动最新 DMG 安装版扫码。
3. 若确认后仍不跳转，检查登录窗口是否关闭、主窗口是否收到认证状态，并查看脱敏后的主进程日志；优先追踪 token 请求、Authorization 捕获和 `login-success` IPC。
4. 首次登录通过后立即退出并重启，确认本地会话恢复。
5. 保持运行并在令牌预计过期前后复测，记录刷新路径是 refresh token 成功还是隐藏会话兜底成功。
6. 如果需要做版本差异定位，以用户已确认可登录的 `0.50` 对应提交/构建为基线逐项比较，不要重新引入 CDP 响应拦截。

### 登录模块完成条件

- 扫码确认后 10 秒内主窗口进入文件列表，登录窗关闭或隐藏。
- 重启应用无需再次扫码。
- access token 过期前能自动刷新，刷新后真实文件请求成功。
- refresh token 不可用时，隐藏官方会话可恢复；只有官方会话也失效时才要求扫码。
- 认证网络失败不会卡死请求，界面会显示可重试错误。
- 日志中不出现完整 token、cookie、手机号或账号敏感信息。

## 6. P0：必须补齐的可用性功能

- [ ] 完成 `0.55.14`（或最近安装的 `0.55.13`）首次扫码、自动跳转、重启恢复的真实账号验收。
- [ ] 完成跨数小时/令牌到期的自动续期验收，并增加可控过期时间的集成测试。（已补 `test/token-expiry.test.js` 可控过期时间测试；跨数小时续期仍需真实账号验收）
- [x] 为登录状态机增加清晰状态：等待扫码、已确认、交换令牌、加载账号、成功、可重试失败。
- [ ] 真实账号逐项验证列表、目录进入、搜索、重命名、移动、复制、删除和回收站恢复。
- [ ] 上传补齐进度、取消、失败重试、大文件、并发限制和重名处理。
- [x] 下载补齐进度、取消、重试、断点续传、链接过期刷新和落盘校验（代码与自动测试完成，真实大文件仍需验收）。
- [x] 音视频补齐常见编码错误提示、播放 URL 过期恢复、字幕加载及拖动后继续播放（代码与自动测试完成）。
- [x] 图片预览补齐大目录分页、预加载边界、损坏文件与网络失败处理（代码与自动测试完成）。
- [ ] 分享完成创建、取消、过期、访问权限和错误路径真实验收。
- [ ] 离线下载完成任务查询、状态刷新、暂停、继续、重试与删除。（任务查询/状态刷新（`offlineTaskState` 状态模型 + `_state` 富化）/去重/删除已可验证，用户已确认“添加、删除”在真实环境可用；剪贴板磁力链自动下载已实现（见 0.55.14 备注）。暂停/继续/重试：外部研究显示 rclone、AList、pikpak-js-sdk 三套实现均只有 create/list/delete、无控制端点，需先用真实账号抓包确认官方客户端如何暂停/继续/重试后再实现，禁止猜测。开发计划见 0.55.14 备注。）
- [x] 保存脱敏后的关键 API fixture，覆盖正常、401、限流、字段缺失和接口变化（`test/fixtures/api/*.json` + `test/fixtures.test.js` 契约测试）。
- [x] 对 token、cookie、下载 URL 和日志做统一脱敏审计。

## 7. P1：体验与维护性完善

- [x] 为文件列表加入符合桌面习惯的右键菜单。
- [x] 增加文件/目录属性对话框，包括大小、类型、创建时间、修改时间和可用媒体信息。
- [x] 增加字幕字号、颜色、背景和延迟设置。
- [x] 优化超过 60 张图片时的连续浏览和缓存淘汰。
- [x] 建立统一传输中心，集中展示上传、下载和离线任务。
- [x] 搜索加入分页、请求取消、缓存失效和高亮。
- [ ] 完成外部播放器的 VLC、PotPlayer、mpv 实测并处理路径含空格场景。
- [ ] 把 `src/App.vue` 按文件列表、搜索、传输、设置等领域拆分。
- [ ] 把 `electron/main.cjs` 的认证、文件 API、下载、上传和窗口管理拆成模块。
- [x] 在设置页或关于页显示版本号、构建时间与日志目录。
- [x] 统一空状态、加载状态、可重试错误和破坏性操作确认样式（用可复用的 `confirm-dialog` 替换全部 6 处原生 `window.confirm`，并统一 `.state/.transfer-empty/.archive-state/.offline-state` 色调；`test/ui-structure.test.js` 覆盖）。

## 8. P2：发布与工程化

- [ ] 生成并验证 Windows 安装包，保留便携版作为诊断渠道。
- [ ] 配置代码签名，减少 SmartScreen 拦截。
- [ ] 设计自动更新、更新失败恢复和版本回滚。
- [x] 建立 CI：单元测试、构建、IPC 核对、打包和产物上传。
- [x] 增加崩溃报告与用户可主动导出的脱敏诊断包（`logger.recordCrash` 捕获 `render-process-gone`/`child-process-gone`/`uncaughtExceptionMonitor`/`unhandledRejection`，写入 `logs/crashes/*.json`，并附带进 `buildDiagnostics` 的 `crashReports` 供导出；主进程未捕获异常只旁路记录，不抑制 Node 默认致命退出；`test/logger.test.js`、`test/ui-structure.test.js` 覆盖）。
- [x] 增加主要文件操作、排序、搜索、状态提示及图片/视频查看器的键盘操作和无障碍标签。
- [x] 增加跟随系统、浅色和深色主题，设置可持久化并响应系统外观实时变化。
- [ ] 增加国际化，并用 macOS VoiceOver/Windows Narrator 完成人工验收。（需做多语言文案提取，人工作验收需真实 VoiceOver/Narrator 设备，本轮未做。）
- [x] 做大目录、长播放列表和多任务传输的性能测试（`test/scale.test.js`：`mergeShareFiles`/`normalizeShareList`/`recentFilesFromEvents` 数千条、`detectConflicts` 线性查表、`generateUniqueName`、`playbackSourcesFromFile` 去重、多页 `filesFrom`/`nextPageToken` 累积；`test/ui-structure.test.js` 锁定搜索 50000/500/5000 与查看器 5000 上限。真实账号大目录与多任务仍人工验收）。
- [x] 做 Electron 安全审计：导航限制、外链、CSP、权限、IPC 输入和路径穿越。
- [x] 增加单实例与多版本同时运行提示，减少用户误测旧版本。

## 9. 代码结构速查

- `src/App.vue`：主界面、导航、文件列表和多数渲染进程业务；当前偏大，是后续拆分重点。
- `electron/main.cjs`：窗口、官方登录、认证、PikPak API、上传下载及大多数 IPC。
- `electron/core.cjs`：纯函数与协议辅助逻辑，新增可测试逻辑优先放这里。
- `electron/preload.cjs`：主界面安全桥接；新增 IPC 时必须同步核对 main handler。
- `electron/viewer.js`：图片、音频、视频预览及播放列表逻辑。
- `electron/viewer-preload.cjs`：预览窗口安全桥接。
- `test/*.test.js`：Vitest 单元与静态契约测试。
- `scripts/verify-ipc.mjs`：构建前核对 preload 暴露与 main IPC 注册。
- `scripts/verify-dist.mjs`：构建后核对 Electron 相对资源路径。

### 0.55.8 最近交互修正

- macOS 目录双指横滑采用手势锁与 90ms 分段；到达根目录后继续横滑会被吞掉，不交给 Chromium 页面历史。
- 图片横滑阈值降为 35、分段间隔 60ms；普通双指滚动不缩放，只有捏合手势缩放。
- 视频捏合缩放范围为 50%–300%；播放控制条已从视频画面独立，缩放不会改变进度条尺寸。
- 图片、视频和音频手势均有自动化契约测试，但触控板手感仍应由用户在真实 macOS 设备上复验。

### 0.55.9 多选交互改版（本 Agent 变更）

- 移除了列表顶部的 `list-batch-actions` 文本操作条，改为选中后在文件面板底部居中的深色圆角浮层图标操作条（`.batch-toolbar`）：分享、收藏、下载、移动、复制、回收站/删除、永久删除、恢复、复制链接、取消分享等按模式显示，末尾固定“取消选择”。
- 图标用内联 SVG `symbol` + `<use href>`（未使用 `v-html`，以满足 `ui-structure` 测试的“不用 v-html”约束），`currentColor` 继承按钮白色。
- 多选交互升级：保留 Command/Ctrl 点选与 Shift 范围选，新增鼠标拖拽框选（`onFilePanelMouseDown`/`onBoxMouseMove`/`onBoxMouseUp`/`applyBoxSelection`），框选时绘制 `.box-select` 半透明矩形，按 Command/Ctrl 框选为追加选择。
- `.file-panel` 是 `position:relative; overflow:auto; height:calc(100vh-120px)` 的内部滚动容器，因此绝对定位的浮层与框选矩形固定于可视面板，不随列表滚动。
- 文件行新增 `:data-id` 用于框选命中；面板新增 `@dragstart.prevent` 防止缩略图拖拽产生“幽灵”图片干扰框选。
- 按用户要求移除了列表表头的“全选/取消全选”复选框（`head-check`）及其逻辑（`toggleSelectAll`、`allSelected`、`someSelected`）与 `.head-check` 样式；选择仍可通过 Command/Ctrl 点选、Shift 范围选、鼠标框选与 Ctrl/Cmd+A 完成，表头 4 列（名称/大小/修改时间/操作）与行网格对齐。
- 按用户要求整列删除“操作”：移除表头 `<span>操作</span>` 与每行的 `.row-actions` 按钮列，网格改为 3 列（名称/大小/修改时间）；打开/属性/下载/收藏/分享/复制/剪切/重命名/回收站/恢复/删除/复制链接/取消分享等改由右键菜单（已在菜单中补充收藏、分享、保存到网盘）、底部多选浮层、双击（打开/进入）、Enter/Space、F2（重命名）、Delete（回收站）完成。
- 交互手感（框选阈值 4px、浮层底部位置）应由用户在真实 macOS 设备上复验；对应结构测试在 `test/ui-structure.test.js`。

### 0.55.9 目录加载修复（收藏/最近面包屑误传“哨兵 id”导致 `drive:list` 报 File or folder is not found）

- **根因**：从「收藏」/「最近」页进入某目录后，`pathStack` 里保留着哨兵项 `{id:'starred'|'recent', ...}`（收藏/最近），面包屑渲染为“收藏 > 目录”。点击“收藏”面包屑时 `navigateCrumb` 在 `mode==='drive'` 下直接 `loadDrive(target.id)`，把 `'starred'`/`'recent'` 当作真实 `parent_id` 传给 `/drive/v1/files`，服务端返回 `File or folder is not found`。「我的文件」根目录 id 为 `''`，所以不会触发。
- **修复**：新增 `SENTINEL_IDS`/`isSentinelId`/`loadSentinelPage`，`navigateCrumb` 与 `loadDrive` 在遇到哨兵 id 时改走对应页面加载器（`loadStarred`/`loadRecent`/`loadTrash`/`loadMyShares`/重置搜索页），不再用哨兵 id 请求 `drive:list`。
- **健壮性**：`core.cjs` 新增 `isFileNotFound(status,data)`（404/410 或含 not found/不存在 等文案）；`apiRequest` 给错误打上 `fileNotFound` 标记并给出友好中文提示；`drive:list` 对父目录不存在返回 `{files:[],notFound:true}` 交由渲染层回退到全部文件 + 提示；`drive:search` 遍历遇到已失效目录跳过而不中断整体搜索。
- 自动化测试：`test/fixtures.test.js`（`isFileNotFound`/友好文案）、`test/ui-structure.test.js`（哨兵分派与 notFound 标记）。真实账号仍需在收藏页进入目录再点面包屑复验一次。

### 0.55.9 项目审查与故障诊断加固

- 主界面加载失败不再把 Chromium 返回的错误描述插入 `data:` HTML，改为加载打包内的 `electron/error.html`、`error.css` 和 `error.js`；页面使用严格 CSP，错误码通过 `textContent` 显示，并防止备用页加载失败时递归重载。
- 主进程未捕获异常改用 `uncaughtExceptionMonitor` 旁路记录：诊断文件仍会生成，但不会因为安装普通 `uncaughtException` 监听器而改变 Node 默认致命退出语义。
- Electron 渲染器及 GPU/工具等子进程异常退出会写入脱敏崩溃报告，并进入用户可导出的诊断 JSON。
- 本轮自动验证：8 个 Vitest 文件、116 项测试通过；生产构建、64 个 IPC 通道核对、2 个 Electron 相对资源检查、JavaScript 语法检查与 `git diff --check` 通过。
- `0.55.13` macOS arm64 的 DMG/ZIP 使用 `node_modules/electron/dist` 本地运行时完成打包，并通过镜像、压缩包及冷启动验证。未签名、未公证，真实账号和长时间续期仍未据此验收。

### 0.55.10 无障碍语义补齐

- 当前目录、全盘搜索、分享链接、离线下载地址和 Access Token 输入框加入明确可读名称。
- 文件列表区域、每行文件及选择框暴露名称和选中状态；排序按钮会读出当前方向以及下一次操作方向。
- 批量操作浮层声明为工具栏，所有纯图标按钮加入独立操作名称；加载、通知、认证状态和错误使用 `status`/`alert` 及 live region。
- 图片查看器的前后切换、缩放/旋转工具栏与幻灯片状态，以及视频查看器的播放控制、工具栏、自动连播和异步状态均加入屏幕阅读器语义。
- 改动只补充语义属性和测试，没有改动用户已经完成的自然排序算法，也没有改变现有鼠标、键盘或触控板处理逻辑。

### 0.55.11 深浅主题

- 设置页新增“跟随系统 / 浅色 / 深色”，通过现有设置文件持久化；旧设置自动迁移为“跟随系统”，非法值也会回退。
- 跟随系统模式监听 `prefers-color-scheme` 变化，无需重启即可实时切换；显式浅色或深色不会被系统变化覆盖。
- 深色配色覆盖侧栏、工具栏、文件列表、传输中心、设置、搜索、右键菜单、各类对话框和状态提示。
- 已通过自动化契约、生产构建和实际深色截图检查；当前只覆盖主窗口，查看器本身原有即为深色媒体界面。

### 0.55.12 栏目状态与 macOS 窗口恢复

- 我的文件、收藏、最近、我的分享和回收站在首次加载后保存各自的内存快照；从其他栏目切回时立即恢复，不再重复请求和闪现“正在加载”。
- 工具栏“刷新”、F5/Cmd+R 和原生菜单“刷新当前页面”仍会主动重新请求；刷新范围补齐到收藏、最近、我的分享、回收站和传输中心。
- 退出登录或会话失效会清空所有栏目快照，避免新账号看到旧账号的文件状态。
- macOS 主窗口点击关闭后改为隐藏并保留 Vue/文件状态；从 Dock 重新激活时直接显示并聚焦原窗口。真正退出应用时通过 `before-quit` 放行窗口销毁和历史落盘。
- 增加本地化原生菜单：打开分享、编辑、刷新、传输中心、设置、全屏、窗口及帮助；应用命令通过受限 preload 事件传递，不使用整页 reload。

### 0.55.13 应用内输入对话框

- 新建文件夹、重命名和压缩包密码不再调用阻塞式 `window.prompt`，统一使用应用内输入对话框。
- 对话框支持 Enter 提交、Escape/点击背景取消、自动聚焦、重命名时选中原名称，以及密码隐藏输入。
- 压缩包密码对话框使用更高层级显示在压缩包浏览窗口之上；取消密码输入时会正确结束等待，不留下未解决 Promise。
- 对话框同时适配浅色、深色主题，并带 `dialog`、`aria-modal` 和动态可读名称。

### 0.55.14 规模测试与更新门控基础（本 Agent 变更）

- 为「大目录/长列表/多任务」补齐自动化规模与健壮性测试（`test/scale.test.js`，7 项）：数千条 `mergeShareFiles` 去重、`recentFilesFromEvents` 折叠为每 id 最新一条且按时序倒序、`normalizeShareList` 完整映射、多页 `filesFrom`/`nextPageToken` 累积不重不漏、`detectConflicts` 用 Map 线性查表、`generateUniqueName` 在大集合中求唯一名、`playbackSourcesFromFile` 多媒体源去重。均在毫秒级完成，说明核心路径未退化为 O(n²)。
- `test/ui-structure.test.js` 新增源码级上限守卫：全盘搜索的 `visited<5000`/`scanned<50000`/`matches<500`、图片查看器 `slice(0,5000)`、文件列表 `renderedLimit` 懒加载。
- 为「自动更新」的任务 `#162` 增加可测试的基础：`core.cjs` 新增 `compareVersions(a,b)` 与 `isUpdateAvailable(local,remote)`（纯函数，`test/version.test.js`，3 项）。真正的更新下载/安装、失败恢复与回滚仍需更新服务器与签名，未在本轮实现。
- 未做：国际化多语言提取（`#167`）、离线下载暂停/继续/重试（`#140`，需真实接口字段后在 main 封装，避免猜测）、`App.vue`/`main.cjs` 拆分（文档明确暂缓）、以及所有需真实账号/硬件/签名的验收项。
- 验证：10 个 Vitest 文件、128 项测试通过；`npm run build`（64 通道 IPC 核对、HLS 打包、相对路径核对）通过；`git diff --check` 通过。

### 0.55.14 离线下载完善（本 Agent 变更 + 开发计划）

**外部研究结论（已用 curl 核实源码）**

- rclone `backend/pikpak`、AList `internal/offline_download/pikpak` 与 `drivers/pikpak`、chris-lsn/pikpak-js-sdk 三套主流实现里，PikPak 离线任务**仅实现 create/list/delete**，均未出现 pause/resume/retry 的控制端点。
- 任务字段/phase 已确认：任务对象含 `id/task_id`、`name`、`type:'offline'`、`file_id`、`file_name`、`file_size`、`message`、`phase`（`PHASE_TYPE_RUNNING/COMPLETE/ERROR/PENDING` 等）、`progress`、`params.url`、`reference_resource`；列表接口 `GET /drive/v1/tasks?type=offline`，可用 `phase.in`/`filters` 过滤；删除 `DELETE /drive/v1/tasks?task_ids=..&delete_files=false` 不删云端已保存文件。
- 结论：官方客户端虽有暂停/继续/重试 UI，但后端动作接口未被上述开源实现记录；**按文档规则 #9 不得猜测**，必须先抓包确认。

**本轮已实现（可验证）**

- `core.cjs` 新增 `offlineTaskState(task)`（phase→`{state,label,percent,isTerminal,canRetry}`，纯函数，`test/offline.test.js` 覆盖）与 `buildOfflineTaskAction(action,id)`（只有已证实的 `delete` 返回请求描述；pause/resume/retry 抛「暂未实现：需先在真实账号下核对」）。
- `main.cjs` `offline:list` 为每条任务附加 `_state`；渲染层 `offlinePhase/offlinePercent` 优先使用 `_state`，回退到原始 phase 计算。
- 未接线任何猜测型 pause/resume/retry IPC 或按钮（`test/ui-structure.test.js` 断言 `preloadSource` 不含 `offlinePause/Resume/Retry`）。

**后续开发计划（按优先级）**

1. **抓包确认**（阻塞项）：在官方 PikPak Web/移动端暂停/继续/重试一个离线任务，用 DevTools/mitmproxy 记录真实请求（含认证头外的 method/path/body）。目标大概率在 `/drive/v1/tasks` 或其 `:xxx` 动作端点。
2. 把确认到的请求补进 `core.cjs` 的 `buildOfflineTaskAction`（集中封装，一处可改），并补形状断言测试。
3. `main.cjs` 新增 `offline:action`（或 `offline:pause/resume/retry`）handler：用 `offlineTaskState` 校验任务状态，用封装的 spec 调 `apiRequest`，异常脱敏。
4. `preload.cjs` 暴露对应桥接（必须与 main handler 成对，`verify-ipc` 会核对）。
5. 渲染层在离线任务行加「暂停/继续/重试」按钮，按 `task._state` 控制可用性，点击后 `refreshOfflineTasks`。
6. **重试的可靠替代**（若确认无独立 retry 端点）：用已证实的 create 端点重发失败任务的 `params.url`（先 delete 失败任务再 create），实现 `offline:retry`；需确认任务列表是否稳定返回 URL 与父目录。
7. 可选：按 `phase.in` 过滤减少查询量；大任务量分页上限与异常提示；离线任务响应脱敏。

**验收原则**：真实账号下创建→状态刷新→删除；pause/resume/retry 必须在抓包确认并实现后，再用真实账号逐项验收，不得以测试桩通过作为完成依据。

### 0.55.14 剪贴板磁力链自动离线下载（本 Agent 变更）

- 新增剪贴板监视：主进程每 2.5 秒读取剪贴板（`clipboard.readText()`），用 `core.cjs` 的 `findMagnetLink` 提取带 `xt=urn:` 的有效磁力链接；检测到且尚未处理过、已登录、设置未关闭时，用已证实的 `driveMutation('/drive/v1/files')` + `buildOfflineTaskPayload` 自动创建离线任务（保存到默认下载目录）。轮询直接识别当前内容，不依赖文本变化事件，因此相同内容重新复制及启动时已有的新链接也能被处理。
- 去重记录保存到用户数据目录的 `clipboard-magnets.json`，按 `xt` 身份识别（忽略 tracker、显示名和大小写差异）并最多保留 500 项，避免轮询或重启重复创建；检测日志不记录磁力链接或哈希内容。
- 失败恢复：创建失败按 5 秒、10 秒退避最多重试 3 次；最终失败会通知渲染层显示可操作提示，用户稍后重新复制即可再次尝试。失败任务不会进入成功去重集合。
- 通知：通过 `offline:clipboard` 事件只推送成功状态和任务（不再向渲染层暴露完整磁力链接）；成功时刷新离线列表，最终失败时显示重新复制重试提示。
- 开关：设置页新增「剪贴板磁力链自动离线下载」（`settings.clipboardMagnet`，默认开启，经 `normalizeSettings` 持久化）；主进程据此决定是否触发。
- 生命周期：`app.whenReady` 启动监视，`before-quit` 清除定时器；`preload` 通过 `onOfflineClipboard` 暴露事件订阅。
- 仅对磁力链自动触发；普通 HTTP/ED2K 链接不自动触发，避免误下载普通 URL。真实账号需复验：复制一个磁力链 → 剪切板轮询周期内自动出现离线任务。

### 0.55.14 外部搜索（u9a9.org）

- 侧边栏新增原生「外部搜索」页，不使用内置浏览器。主进程按 `https://u9a9.org/?type=2&search=关键词` 请求 HTML，只向渲染层返回解析后的标题、磁力地址和 `xt` 身份。
- 请求限制关键词为 2–100 字符、15 秒超时、5 MB 响应上限和最多 200 条结果；结果按 BTIH/BTMH 身份去重，避免同一资源因 tracker 不同重复出现。
- 每条结果支持「复制磁力」和「保存到网盘」；复制由主进程安全写入剪贴板，保存复用已验证的离线任务创建接口并要求已登录。
- 自动解析与 IPC 契约测试、构建检查通过；已用 Electron 实际请求和渲染 u9a9 搜索结果，确认表格行中的标题与磁力地址可以解构。界面显示标准 `magnet:?xt=urn:btih:...` 主体，复制/保存仍使用包含 tracker 的完整地址；标题最多两行，结果区限制在窗口可用高度内并独立滚动，长结果不会横向或纵向撑大窗口。

### 0.55.14 离线面板布局修复（删除任务后拉伸）

- 现象：删除离线任务后，离线面板同时显示“正在加载…”与旧任务行（`run()` 会把全局 `loading` 置真，而任务列表 `v-for` 无条件渲染），造成上方大段空白 + 下方任务行的“被拉伸”观感；上传/下载面板没有 `loading` 分支，所以只有离线面板出现。
- 修复：离线面板“正在加载…”改为 `v-if="loading&&!offlineTasks.length"`（有任务时不叠着加载文案）；`.transfer-center` 加 `align-items:start`（各栏目保持内容高度、不拉伸）；`.offline-row .download-info` 改为紧凑纵向 flex 居中，去掉进度条多余外边距。
- 测试：`test/ui-structure.test.js` 断言上述三点。

### 0.55.14 音频播放恢复与外部搜索

- 音频播放器补齐与视频一致的过期地址恢复：发生媒体错误时通过 `viewer:media-refresh` 刷新真实播放地址，保留播放进度、音量、静音、倍速和播放/暂停状态，不再直接销毁播放器显示整页错误。
- 音频增加“正在缓冲”“网络较慢”状态提示，并同步播放状态到主进程的防休眠管理；失败提示保留播放器以便用户恢复。
- 外部搜索最终采用原生搜索结果页，不加载远程浏览器页面。
- `test/viewer.test.js` 增加音频过期地址恢复回归测试；`test/ui-structure.test.js` 覆盖外部搜索 IPC、请求参数和移除内置浏览器分区。

### 0.55.15 Windows 便携版与现有功能体验完善

- 会话的 refresh token 与隐藏官方会话均无法续期时，主进程会把认证状态切换为可重试错误；连接页明确显示“登录状态已过期，请重新连接 PikPak”，不再只清空文件列表而缺少恢复指引。
- 文件列表、收藏、最近、我的分享、回收站及分享目录加载失败时，错误状态增加就地“重试”按钮，复用当前页面语义重新请求。
- 未登录时将全盘搜索、收藏、最近、我的分享和回收站明确置灰并提示“连接账号后可用”；通用刷新按钮只在已登录文件页或已打开的分享目录显示；刷新与账户按钮在请求处理中禁用并显示处理中状态，避免无效操作和重复提交。
- 图片查看器响应鼠标滚轮：向上切换上一张、向下切换下一张，并用 60ms 手势锁避免单次滚动连续跳过多张；Ctrl/Command + 滚轮继续缩放。横向触控板切图、普通双指纵向滚动不缩放及捏合缩放的既有语义保持不变。
- 文件列表仅在首次加载且没有内容时显示整页加载/错误状态；后台刷新会保留已有文件行并显示吸顶的紧凑进度条，失败时就地提供重试，不再让全高状态区与旧列表同时渲染造成跳动或拉伸。
- 所有选项卡顶部工具栏均移除重复的“打开分享”按钮；打开分享统一从左侧“分享链接”进入，分享页内仍保留链接输入表单。全盘搜索和外部搜索将重复面包屑替换为用途说明；两页统一为居中、限宽的搜索布局，搜索词不足 2 个字符时禁用提交，外部搜索使用独立搜索卡片、结果容器和空状态，深色主题同步适配。
- Windows 打包新增 `scripts/package-win.cjs`：优先复用 `node_modules/electron/dist`，其次使用校验过大小且版本/架构匹配的 `@electron/get` 缓存 ZIP，均不可用时才联网下载；产物按版本写入 `release-<版本>-win`，避免 TLS 波动、旧解包目录被扫描锁定或不同候选版本互相覆盖。
- 冷启动烟雾测试使用系统临时目录中的一次性独立用户资料，结束后自动清理；不会读取日常账号、token、导航历史或文件名，也会把非零进程退出码视为失败。
- 自动验证：11 个 Vitest 文件、155 项测试通过；67 个 preload/main IPC 通道、生产构建、4 个 Node 语法检查及 `git diff --check` 通过。
- Windows x64 便携版：`D:\dev\pikpak\release-0.55.15-win\PikPak-Desktop-0.55.15-portable.exe`
  - 大小：`105508807` 字节
  - SHA-256：`8bbe81c883c630e14e32b97c7dc8bd16db617ec3a87cab9911ed1cb72cf7a4c6`
  - 文件版本：`0.55.15`
  - Authenticode：未签名；仍可能触发 SmartScreen，不能视为代码签名完成。
- 隔离冷启动截图：`D:\dev\pikpak\release-0.55.15-win\smoke-0.55.15.png`，已人工确认显示未登录连接页且无真实账号数据。
- 搜索页布局截图：`D:\dev\pikpak\release-0.55.15-win\global-search.png` 与 `external-search.png`，已人工确认顶部无“打开分享”、标题/说明无重复且卡片和结果区没有溢出。
- 尚未完成：Windows 真实账号扫码、重启恢复、文件操作、播放与传输验收；跨数小时自动续期；VLC/PotPlayer/mpv 实测；代码签名、自动更新与回滚。这些项目不能由隔离烟雾测试或测试桩替代。

### 0.55.16 全局移除顶部“打开分享”

- 从所有选项卡的顶部工具栏彻底移除“打开分享”，避免与左侧固定的“分享链接”入口重复；分享页内的链接输入表单保持不变。
- `test/ui-structure.test.js` 将顶部工具栏切片后断言不再包含 `showShareStart`，同时锁定侧边栏分享入口仍存在。
- 自动验证：11 个 Vitest 文件、155 项测试通过；67 个 preload/main IPC 通道、生产构建、Node 语法检查及 `git diff --check` 通过。
- Windows x64 便携版：`D:\dev\pikpak\release-0.55.16-win\PikPak-Desktop-0.55.16-portable.exe`
  - 大小：`105508484` 字节
  - SHA-256：`a574c55b9207b2d9d70cedfac298780012a8aea34ea69ca56c7588e2f26beac6`
  - 文件版本：`0.55.16`
  - Authenticode：未签名。
- 隔离冷启动截图：`D:\dev\pikpak\release-0.55.16-win\smoke-0.55.16.png`，已人工确认顶部不再显示“打开分享”，且没有真实账号数据。

### 0.55.17 外部搜索任务计数即时刷新

- 外部搜索结果“保存到网盘”成功后立即调用统一的 `refreshOfflineTasks()`，无需打开传输中心即可刷新侧边栏任务总数；刷新仍复用 `offlineRefreshPromise` 去重，避免并发查询。
- `test/ui-structure.test.js` 锁定创建任务后再刷新列表的调用顺序，并确认侧边栏计数继续由上传、下载、离线任务三类数据合计。
- 自动验证：11 个 Vitest 文件、156 项测试通过；67 个 preload/main IPC 通道、生产构建、隔离冷启动及 `git diff --check` 通过。
- Windows x64 便携版：`D:\dev\pikpak\release-0.55.17-win\PikPak-Desktop-0.55.17-portable.exe`
  - 大小：`105507394` 字节
  - SHA-256：`8fd5cf1f72ce4fa3dbb6a93cb954c3723e2edfb7ce13c3e0f4623779854c5961`
  - 文件版本：`0.55.17`
  - Authenticode：未签名。

### 0.55.18 全盘搜索可用性优化

- 全盘搜索侧边栏入口不再因未登录直接置灰；用户可以进入页面查看所需条件，未登录时页面提供明确的“连接 PikPak 账户”按钮。
- 已登录时显示原有全盘搜索表单；少于 2 个字符或登录状态不满足时给出明确错误，不再静默返回。每次重新进入搜索页会清理其他页面遗留的错误提示。
- 修复“明确存在的文件仍无结果”：递归遍历的每次目录请求补齐 `with_audit=true`，并只读取已完成、未进回收站的文件；这些参数现在与已验证的 PikPak 文件列表请求一致。
- 文件夹递归兼容 `kind=drive#folder` 和 `mime_type=application/x-directory` 两种目录标记，避免遇到后者时整棵子目录被漏搜；查询词与文件名统一使用 NFKC 和大小写归一化，可正确匹配全角/半角等价名称。
- 仅缓存有命中的完整搜索，不再缓存空结果，避免文件刚上传、刚保存或由其他客户端新增后继续被旧的零结果遮挡。
- 新增单元与结构回归测试，锁定入口可访问、登录引导、输入校验、目录兼容、名称归一化、有效文件筛选和空结果不缓存。
- 自动验证：11 个 Vitest 文件、158 项测试通过；67 个 preload/main IPC 通道和生产构建通过。真实账号下的完整目录遍历仍需用户用已知文件名复验。
- Windows x64 便携版：`D:\dev\pikpak\release-0.55.18-win\PikPak-Desktop-0.55.18-portable.exe`
  - 大小：`105508917` 字节
  - SHA-256：`b25f64b10352a6678bd8465080514b3ef40553c83a8ac3a2e491827b58037111`
  - 文件版本：`0.55.18`
  - Authenticode：未签名。
- 隔离冷启动截图：`D:\dev\pikpak\release-0.55.18-win\smoke-0.55.18.png`；未登录全盘搜索页截图：`D:\dev\pikpak\release-0.55.18-win\search-0.55.18.png`。两次均使用临时用户资料，未读取日常账号。

## 10. 代码风格与实现规则

1. Electron 主进程、preload 与 core 使用 CommonJS；Vue 源码和测试沿用现有 ESM/Vite 方式。
2. 遵循相邻代码风格；现有文件有较多紧凑写法，但新增复杂逻辑应使用清晰的多行结构。不要顺手格式化无关代码。
3. 可独立验证的解析、状态计算、路径判断和兼容逻辑放入 `electron/core.cjs`，同时添加 Vitest。
4. 新增 IPC 必须成对修改 preload 与 main，并确保 `npm run build` 的 IPC 核对通过。
5. renderer 传入的 ID、URL、文件名、播放器路径和选项均视为不可信输入，在 main 进程校验。
6. 网络错误要保留可诊断的状态码和上下文，但展示与日志都必须脱敏。
7. Electron `webRequest` 监听器只要声明了 callback，就必须在所有控制流分支调用；涉及解析的代码使用 `try/finally` 保证放行。
8. 登录流程不要引入 CDP 响应拦截，除非已用独立最小实验确认不会影响官方页面，并有明确回滚开关。
9. token 优先用 `safeStorage` 或现有安全存储路径；不要写入源码、测试 fixture、截图或普通日志。
10. 本地文件路径必须防路径穿越；删除、覆盖和批量移动前确认解析后的绝对路径在预期目录内。
11. 播放列表和大目录继续采用懒加载/分页，避免一次拉取全部媒体。
12. 异步请求需考虑页面切换、窗口关闭、旧请求覆盖新状态以及 timer/listener 清理。
13. 每个“已完成”结论必须附验证证据：测试命令、真实账号场景、产物路径或日志现象。

## 11. 标准执行方式

### 接手时先运行

```bash
cd '/Users/xu/Documents/pikpak'
git status --short --branch
git diff --stat
npm test
npm run build
```

先阅读本文件和相关 diff，确认哪些改动是用户已有工作。不要为获得“干净工作树”而还原文件。

### 开发运行

```powershell
npm install
npm run dev
```

调试登录时记录实际进程的完整可执行路径和版本。修改认证逻辑后必须至少执行一次全新登录与一次重启恢复，不能只看 DevTools 或自动化测试。

### 当前 macOS 打包方式

```bash
npm run package:mac
```

默认生成 Apple Silicon DMG 和 ZIP 到 `release/`。当前机器没有有效的 Apple Developer ID，因此产物未签名、未公证。若 DMG 偶发报 `hdiutil: create failed - 资源暂时不可用`，先用 `hdiutil info` 确认没有本次构建残留挂载，再基于已生成应用重试：

```bash
npx electron-builder --prepackaged 'release/mac-arm64/PikPak Desktop.app' --mac dmg
```

### Windows 打包方式（待在 Windows 主机复验）

标准 `electron-builder` 下载 Electron runtime 时曾出现停滞；当前稳定方式是复用 `node_modules/electron/dist`：

```powershell
npm run build
npx electron-builder --win dir `
  --config.directories.output=release-<version> `
  --config.electronDist=node_modules/electron/dist
npx electron-builder `
  --prepackaged release-<version>\win-unpacked `
  --win portable `
  --config.directories.output=release-<version>-portable
```

打包可能持续 90–150 秒。若命令返回运行中的 session，应继续轮询同一个 session，不要重复启动多个打包进程。

### 每个候选版本发布前

```bash
npm test
npm run build
git diff --check
shasum -a 256 "release/PikPak-Desktop-<version>-mac-arm64.dmg" "release/PikPak-Desktop-<version>-mac-arm64.zip"
hdiutil verify "release/PikPak-Desktop-<version>-mac-arm64.dmg"
unzip -tq "release/PikPak-Desktop-<version>-mac-arm64.zip"
```

然后完成：

- 冷启动烟雾测试，并确认没有后台残留旧版本。
- 真实账号登录、跳转、重启恢复测试。
- 文件列表、至少一个文件操作、一次播放和一次传输冒烟测试。
- 记录版本、完整路径、文件大小、SHA-256、测试数量和未验证项。

版本号用 `npm version <version> --no-git-tag-version` 同步更新 `package.json` 与 `package-lock.json`，不要只改一个文件。

## 12. Agent 交接与提交规则

每次交接至少写清楚：

- 用户当前看到的现象，以及最近一次用户明确确认的版本和行为。
- 本轮修改了哪些文件、为什么改、关键控制流是什么。
- 哪些内容通过了自动测试，哪些必须由真实账号或时间跨度验证。
- 最新产物的绝对路径、版本、大小和哈希。
- 工作树中哪些改动不是本轮创建的，避免覆盖。
- 下一位 Agent 可以直接执行的第一条命令和最小复现步骤。

提交前：

- 使用 `git diff` 检查范围，只提交当前任务相关文件。
- 不提交 `release-*`、日志、截图、账号数据、cookie 或 token；开发交接文档可以提交，但必须保持脱敏。
- 不使用 `git reset --hard`、`git checkout --` 或不明确目标的递归删除。
- 未经用户明确要求，不推送、不创建发布、不覆盖远端历史。

## 13. 推荐的下一阶段顺序

1. 让用户用 `0.55.14` 最近安装版重新扫码，确认首次登录和自动跳转。
2. 若失败，仅围绕认证状态机和 token 请求放行做最小差异定位，先恢复可登录性。
3. 登录通过后立即验证重启恢复，再进行跨数小时自动续期测试。
4. 补充可控 token 过期的集成测试，避免每次都等待数小时。
5. 完成上传、下载和播放的真实账号 P0 验收。
6. 再拆分 `App.vue` 和 `main.cjs`，不要在登录仍不稳定时做大规模重构。
7. CI 和 macOS 安装包已具备；最后推进 Windows 复验、Apple/Windows 签名、自动更新和回滚。
